# PROGRESS · 活动记录生成器

> 北极星:CO 活动数据进 → 一张月历留痕(暖拓质纸 + 朱砂里程碑印)+ 印刷级 PDF 出;将来作 CO 的活动记录组件。
> 本文件每个里程碑追加一条(日期 · 改了什么 · 状态 · 新增卡点),顾问靠它刷新进度台。最新在最上。
>
> **护栏**:纯 render+export 组件(进活动数组、出留痕+PDF)· 不自建活动存储 · 不发明跟 CO 打架的 schema(占位待 CO 对齐)· 不为并入 CO 做多余改动。
> **物理卡点(降级默认继续)**:缺霞鹜文楷 ttf → 中文先留缺口;缺 npm 工具链机器 → 真交互先用渲染截图/‌node 验。

---

## 台账

### 2026-08-19 · 本轮收尾(F–J 五个里程碑,全推送)
- **本轮方向**(用户 2026-08-19 定):偏「多人对话的秘书」定位(能记录/统计)· 质感要**手工且可编辑**(手绘/拓扑/扎染)· 做成**任何需要统计的地方都能无限复用**的组件 · 本轮不接 CO。
- **落地**:F 质感抽成可编辑模块(四预设,三条渲染路径共用一份定义)· G 统计层(秘书那一半,含按任意字段分组给「多人」留口)· H 可嵌入小件(活动带/统计卡,同页无限个)· I 类型开放(修静默丢格的真 bug)· J 质感调参台(双击就开,拖滑块调,配置抄走)。
- **自测**:质感 59 · 统计 40 · 小件 39 · 类型 30 = **168 项全过**;另有无头 Edge 截图逐张肉眼验收(手绘/拓扑/频率换算三处问题都是这样揪出来重调的)。印刷路径无回归。
- **顺带修掉的**:屏幕 speckle 恒不可见(黑色叠 screen)· 非自带类型静默丢格 · 无 type 活动在图与统计里口径不一 · 分类多时图例冲出版面 · 质感参数两条规则打架导致换载体变样。
- **未做(留给下次或用户定夺)**:单月卡片 PDF 导出 · 旧规划海报死代码清理 · app 壳(App.jsx)还没接上新质感/统计(本机跑不了 Vite,接了也验不了)。
- **物理卡点不变**:① 缺霞鹜文楷 ttf → 印刷 PDF 中文留缺口;② 本机无 npm 工具链 → 真交互未整跑(渲染/统计/小件均已用 node + 无头浏览器验)。

### 2026-08-19 · 里程碑 J:质感调参台(「可编辑」对设计师也成立)
- **改了什么**:① **参数规则收成一条** —— 全套参数按 A1 长条为基准写,**载体只换算噪声频率,别的一律照搬**,且不分默认值还是调用方给的。原先两条规则互相打架(频率只有默认值才换算;透明度另有一套载体倍率又会被显式赋值覆盖),后果是同一份配置换个载体就变样。加 `none` 预设(干净的纸)。② 新增 `design/2026-08-19-texture-module/02-质感调参台.html` —— **自足单文件,双击就开**,不用 npm、不起服务、不联网(质感模块零依赖,源码内联进页面);四预设各配滑块/取色/开关,四种载体随时切,调好直接吐可粘贴配置、一键复制。
- **为什么**:「可编辑」此前只对写代码的人成立(改源码里的参数对象)。
- **所见即所得靠什么保证**:① 参数规则统一后,配置换载体不变样;② 页面里每个载体的 `freqMul` 直接抄自渲染函数,**构建时校验对得上,对不上就报错**,不让一张骗人的页面生成出去(实测发现页面自算的 ×7.0 与组件真用的 ×4.2 不符,就是这样揪出来的)。
- **状态**:✅ 全套自测无回归(质感 59 / 统计 40 / 小件 39 / 类型 30);✅ 无头 Edge **不加任何放宽标志**打开调参台成功渲染(证明自足);✅ 截图比对调参台预览与真组件一致。
- **卡点**:无新增。滑块拖动的交互仍只能人工点(无头浏览器点不了),但渲染路径与配置输出已验。

### 2026-08-19 · 里程碑 I:类型开放(修一个静默丢数据的真 bug)
- **发现的问题**:组件号称「任何需要统计的地方都能无限次复用」,但分类表**写死了 5 个 CO 类型**。实测喂 `{type:'meeting'}`:统计算它有痕、**整年图上那天却是空白**——渲染查不到分类就不落格,而且静默。复用到别的场景(会议/复盘/客服)必中招。
- **改了什么**:① `toRecordModel` 把数据里出现的未登记类型**自动补进分类表**,配矿物真值扩展色(石青/胭脂/雌黄/苍绿/紫毫/秋香);一批类型名→一套颜色(按名哈希落座、占了顺延),同批内互不重复,**与出现顺序、出现次数无关**。② 没写 type 的活动统一归「(未分类)」(`typeOf`),**渲染与统计共用这个口径**(此前统计里是 `''`/`null`、图上没分类,又一处打架)。③ A 全拓下补出来的类型仍走墨色。④ 分类多时图例不再冲出版面(整年/单月/PDF 三处都加截断)。⑤ `aggregateByDay` 加脏数据护栏。
- **状态**:✅ 新增 `scripts/test-open-types.mjs` 30 项全过,核心断言就是不变式本身:「凡统计算作有痕的天,图上必须查得到分类」「统计有痕天数 = 渲染落格天数」。✅ 全套自测无回归(质感 57 / 统计 40 / 小件 39);✅ 印刷路径无回归(两变体仍 847×600mm、211 天落格、4 个里程碑)。
- **卡点**:无新增。

### 2026-08-19 · 里程碑 H:可嵌入小件(活动带 + 统计卡)
- **改了什么**:新增 `src/embed/index.js` —— `renderStrip` 活动带(7 行×周列的紧凑留痕方格,可只画某段月份/自定格子大小/开关月标尺与周标)+ `renderStatCard` 统计卡(关键数字 + 分类占比堆叠条 + 图例 + 一行人话摘要,自带色板与 `summarize`,不必调用方喂)。`createRecord` 加 `stripSVG()` / `statCardSVG()`。附带质感一处修正:**噪声频率随元件尺寸自适应**(原按 A1 定,放到 120mm 卡上拓扑成绸缎光斑而非等高线)。README 重写(新 API + 质感 + 统计)。
- **为什么**:此前只有印刷大件(A1 长条、210×280 月卡),塞不进网页侧栏/仪表盘 —— 「能在任何需要统计的地方无限次复用」落不了地。
- **「无限次复用」怎么保证的**:① 所有 SVG id **按内容派生前缀** → 同页多实例互不撞车,且**同参数同输出**(确定性,便于自测与快照对比),也可显式给 id;② 一律 `width="100%"` + viewBox,由容器定尺寸,**不写死像素**。
- **状态**:✅ node 自测 39 项全过(id 两两无交集/确定性/尺寸自适应/四质感/空与乱输入不崩);✅ **无头 Edge 演示页肉眼验收** —— 同页 14 个实例(仪表盘/侧栏/分季度/按人分组/四质感/行内),互不干扰,并据此发现并修了上面那条频率问题。演示页 `design/2026-08-19-texture-module/01-可嵌入小件演示.html`。
- **卡点**:无新增。

### 2026-08-19 · 里程碑 G:统计层(「秘书」那一半)
- **改了什么**:新增 `src/data/stats.js` —— 纯函数统计:总投入 / 有痕天与留白 / 活跃日均 / **最长与最近连续天数** / 分类分布与占比 / 按月 / 强度分档分布 / 最忙一天 / 里程碑 / 首末日,外加 `summarize()` 出**一行人话摘要**。`createRecord` 加 `stats()` 句柄。
- **护栏怎么守的**:**不发明新 schema** —— 只吃现有占位契约,内部复用 `toDailySeries`,故**统计口径与渲染口径同源**(自测直接断言「有痕天数=渲染落格天数」「分档合计=全年天数」),不会出现两套数字打架。`groupBy` 支持按活动上的**任意**字段分组(将来 CO 给了 `actor` 就能按人分),字段不存在归到「(未标注)」——为「多人」留口但不强加字段。
- **状态**:✅ node 自测 40 项全过。手算小样本**逐个对数**(不只看没崩):5 天有痕/总投入 10/最长连续 3 天/最忙 1/3 等;另验跨年过滤、闰年 366 天、空/null/非数组/含 null 项全归零不抛。
- **卡点**:无新增。

### 2026-08-19 · 里程碑 F:手工质感抽成可编辑模块(拓质/扎染/手绘/拓扑)
- **改了什么**:新增 `src/texture/index.js` —— 一个纹理=一份**可编辑参数对象**,`texture(name, overrides)` 浅合并即改;`build()` 吐 `{defs, body}`;**id 带前缀隔离**,同页可放多个纹理不撞车(可无限次复用的前提)。四预设:`rubbing` 拓质(原味)/ `tiedye` 扎染(染心晕开+湍流揉皱+折痕环)/ `handdrawn` 手绘(铅笔颗粒+方向排线)/ `topographic` 拓扑(噪声阈值化成等高线)。**三条渲染路径(整年长条/单月卡/印刷栅格化)改吃同一份定义**(此前各内联一套、三处重复)= 单一真源,印刷质感不会再跟屏幕漂。`createRecord({texture})` 可整体锁定,`yearSVG({texture})` 可逐次覆盖。新增 `scripts/test-texture.mjs`、`scripts/build-texture-gallery.mjs` + 样品册页。
- **状态**:✅ node 自测 51 项全过(四预设可建/参数可编辑/id 隔离/三路径都吃到/默认拓质不回归);✅ **无头 Edge 截图肉眼验收**,并据此重调了两个预设的参数(见下)。
- **顺带修了个隐藏 bug**:屏幕两处 speckle 滤镜**输出黑色却叠 screen 混合 = 恒不可见**——注释写的「能看见的小飞白」其实一直没显示(栅格化路径用的是白色,两边不一致)。现统一为可见白飞点。
- **取真值(实测重调)**:初版手绘=一堵灰墙、拓扑=迷彩油斑,因为阈值算错——噪声三通道均值≈0.5,阈值必须压过均值否则整页恒过阈;等高线线宽=1/(档数−1),16 档肥成迷彩、48 档才是等高线。扎染总透明度 0.5 会压住数据、0.28 又没手工味,取 0.38。
- **卡点**:无新增。拓质栅格化那条印刷路径参数没动,故印刷验收状态不变。

### 2026-08-14 · 连跑收尾(上一程到干活边界)
- **本轮成果**:A 建模细化+契约文档 · B 单月详情页+视图切换 · C 分类色取真值(矿物色) · D 拓质栅格化加固+浏览器实测 · E 公开组件 API+README+健壮性。全部自测→提交→SSH 直推(main 到 `d7ab092`)。
- **为何停**:原队列①–⑤已全覆盖,自加 E 落实"组件可被调用"。再往下要么撞物理卡点、要么是需用户/CO 定夺的投机项——按"不空转"停,交回执。
- **待用户/CO 定夺的下一批(未做)**:
  - **单月卡片 PDF 导出** —— 现 PDF 只出整年长条;单月详情页(renderMonth)可加印刷导出。价值取决于 CO 是否要"可打印月卡",故留给定夺。
  - **死代码清理** —— 旧「规划海报」路径(`renderPoster.js`、`exportPdf.js` 里的 `buildPosterPdfBytes`/`exportPosterPDF`、`model.js` 的 `sampleModel/loadModel/saveModel`、`scripts/test-pdf.mjs`)已全孤儿、无害但占地。清了组件面更干净;因会动到正在工作的导出文件、且属纯整洁,未擅动。
- **物理卡点(降级默认继续中)**:① 缺霞鹜文楷 ttf → 印刷 PDF 中文留缺口,完整验收待字体;② 本机无 npm 工具链 → Vite 真交互未整跑(渲染截图验、PDF node 验、拓质栅格化无头浏览器实测)。

### 2026-08-14 · 里程碑 E:公开组件 API 入口 + README + 健壮性
- **改了什么**:新增 `src/record/index.js` 单一可调用入口(建模/渲染/导出/占位数据 re-export + `createRecord(activities,{year,variant})` 便捷句柄 `yearSVG()/monthSVG(m)`);README 重写为「活动记录生成器 + CO 调用方式 + 占位契约 + 卡点」。
- **状态**:✅ node 验公开 API + 健壮性 9 项全过:空数组/null/闰年二月/非法 variant 回退/单条/两变体全量都不崩。这落实北极星第三条腿「组件能被 CO 调用」——接 CO = 写 `fromCreativeOS` 适配器塞输入层。
- **卡点**:无新增。

### 2026-08-14 · 里程碑 D:拓质栅格化管线做扎实 + 浏览器路径实测
- **改了什么**:`rasterizeRecordTexture` 加固——任何失败(Image/toBlob/getContext)优雅降级返回 null(不拖垮导出)、加载超时守卫、长边+面积双上限守卫、RGB→CMYK 注释;抽出 `buildTextureSVG`。
- **状态**:✅ **无头 Edge 实测**导入真模块跑栅格化:产出带斑驳+剥蚀的拓质 PNG(72dpi 下 2401×1701px 尺寸精确),对照纯平涂纸明显有别 → ③ 从"代码就绪"升为"真 Chromium 端到端验证"。node 兜底两变体矢量 PDF 无回归(847×600mm)。
- **卡点**:仅剩"PNG 嵌 pdf-lib + 整下载"这一步的浏览器整跑待 npm 环境(embedPng 是标准调用,低风险);印刷完整验收仍等霞鹜文楷 ttf。

### 2026-08-14 · 里程碑 C:分类色系统打磨(取真值·国画矿物色)
- **改了什么**:五类分类色从"几乎同色的死淡"换成矿物真值——设计=藤黄`#c9a15a`/写作=赭石`#b5794f`/研究=花青`#6f8ea0`/构建=石绿`#86a07e`,朱砂印泥`#9e3b32` 抽成 `SEAL` 常量;`activity.js` 加取真值注释,锚点文档记色表。
- **状态**:✅ 整年 B 截图验:五类一眼可辨、强度深浅仍在、落麦纸+拓质后柔和,未滑向 AI「奶油+赤陶」俗脸。
- **卡点**:无新增。

### 2026-08-14 · 里程碑 B:单月详情页 + 整年/单月视图切换
- **改了什么**:新增 `src/poster/renderMonth.js`(单月 7 列周历大图,竖版 210×280mm:楷体报头+月投入统计、周表头周日朱砂、强度墨底、日号老式数字、活动标题可读、朱砂里程碑印、拓质);app 加「整年↔单月」视图切换 + 月份前后导航,整年点某天联动选月;`build-month-preview.mjs` 预览脚本。
- **状态**:✅ 截图验(一月里程碑/三月最忙/八月留白断口 + A 墨版对照,观感成立);node 验 12 月 + mono 全渲染无越界。app 交互(切换/导航)因缺 Vite 未整跑,渲染本身已证。
- **卡点**:app 真交互验证待 npm 工具链(降级:渲染截图已验)。

### 2026-08-14 · 里程碑 A:建模细化 + 数据契约文档
- **改了什么**:`levelFor` 抗离群值(小整数投入量即档、大跨度才线性缩放);留白/墨深/里程碑三条硬规则显式注释;新增 `docs/数据契约-占位.md`(I/O、每日契约形状、渲染模型、**5 个待 CO 对齐点** + adapter 建议)。
- **状态**:✅ node 验:档位分布 L1..L4=73/106/30/2 健康;留白 154 天;里程碑通道 4 条;无 level0 误落格。
- **卡点**:无新增。

### 2026-08-14 · 连跑模式启动(基线)
- **改了什么**:确立 goal 连跑;建本文件。此前两轮已完成:(a) 抢救提交 9 批 + 仓内身份 + SSH 直推;(b) B 接成渲染+导出组件——app 壳改活动预览、`{date,count,level}` 契约建模、PDF 对齐 B(朱砂里程碑+分类色)、拓质≥300dpi 栅格化嵌入(浏览器 canvas 路径)。
- **状态**:✅ 基线在 GitHub(`yunmin311/calendar-app` main,到 `9958851`)。渲染经 B 截图验证、PDF 经 node 两变体验证。
- **卡点**:① 霞鹜文楷 ttf 缺 → 印刷 PDF 中文留缺口,完整验收待字体;② 本机无 node_modules → Vite/真交互跑不了,App.jsx 只经渲染验证。
